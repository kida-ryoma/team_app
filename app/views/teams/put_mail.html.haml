= form_with model: @email, url:send_mail_team_path(@team.id) , class: "Box", local: true do |f|
  .signup
    .signup__name
    = f.label :email, class: "signUpForm__label"
    = f.text_field :email, class: "signupForm1", placeholder: "例) メルカリ太郎"
    = f.submit "次へ進む", class: "signUpBtn"